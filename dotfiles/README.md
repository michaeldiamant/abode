Install via stow like `stow -t ~/ tmux`

Some dependencies must be installed by hand:
* oh-my-zsh plugins in .zshrc.
* npm via asdf, otherwise coreutils won't install.
* https://github.com/tmux-plugins/tpm, otherwise tmux plugins won't activate.
