# abode
Simplifies setting up a new computer with bootstrap scripts and saved configs.

## MacOS Setup

Increase key repeat speed:
* See current value: `defaults read -g KeyRepeat`
* Set new value (as low as 1): `defaults write -g KeyRepeat -int 1`
* Re-run read command to confirm write happened. Restart to take effect.
